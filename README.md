# 🌍 React Native Expo App

This project is built using **React Native** and **Expo**. It can be easily run on any mobile device using the **Expo Go** app — no Android Studio or Xcode setup required.

---

## 🚀 How to Run the App

### 1️⃣ Install Prerequisites
Make sure you have these installed:
- [Node.js](https://nodejs.org/)
- **npm** (comes with Node.js)
- **Expo CLI**

Install Expo CLI globally:
```bash
npm install -g expo-cli
git clone https://github.com/5HAUNzee/NIT_hacks.git
cd NIT_hacks
npm install
npm start
```
Install expo go app on your mobile device from playstore ,scan the QR generated in the terminal after running npm start using scan QR in the app front page
